# Monorepo：從 repo 根目錄部署，實際建置在 frontend/
# Netlify UI：Base directory 留空（.），Publish directory 留空
[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
