# Railway：Settings → Root Directory = backend，Builder = Dockerfile
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "on_failure"
# 不在此設 healthcheckPath，避免探測失敗導致外網 502（X-Railway-Fallback）
